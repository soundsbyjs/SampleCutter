# cut the audio file into equally-spaced parts
# supply the bpm of the file and a fraction to cut the file into chunks

from fractions import Fractions
import ffmpeg
class cutter:
    interval, bpm, length
    def cutter(self, bpm, length, numerator, denominator):
        self.interval =  interval
        self.bpm = bpm
        # have to figure out how to parse this vv
        self.length = fraction(numerator, denominator)
    def calculate_lengths(self):
        while (length = length - (bpm * interval) >= 0:

